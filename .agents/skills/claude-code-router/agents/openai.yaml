interface:
  display_name: "Claude Code Router"
  short_description: "Repo-specific patterns and workflows for claude-code-router"
  default_prompt: "Use the claude-code-router repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true